# Pipeline and visualization configuration.
# Priority: CLI args > environment variables > YAML > defaults.

pipeline:
  dset_path: null
  image_dir: null
  dr: dr11
  cont: false
  model_size: base
  batch_size: 1
  crop_size: [2352, 1176]
  num_workers: 2

visualization:
  image_dir: null
  blob_dir: null
  surveyccd_path: null
  surveyccd_path_dr8: null
